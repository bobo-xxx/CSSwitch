# Verify provider and platform compatibility

Type: task
Status: open
Blocked by: 07

## Question

Run the cross-provider conformance suite and all relevant existing tests, linting, and formatting checks. Confirm Linux headless behavior and macOS source/runtime compatibility; if no macOS runner is available, record that runtime proof as an explicit pending limitation rather than claiming it passed.
